// ABOUTME: Application entry point that renders the React app
// ABOUTME: Sets up root component and mounts to DOM
import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
